//API keys and route are in .env file. Generate new .env file with 
//const API_KEY = ''
//const API_HOST = ''
//Then install npm install dotenv --save
//And load them into the variable below
const dotenv = require('dotenv').config()

const puzzleBoard = document.getElementById('puzzle')
const solveButton = document.getElementById('solve-button')

